# Railway deployment config
# Each service is deployed separately via Railway's monorepo support
# Set the root directory per service in Railway dashboard

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
